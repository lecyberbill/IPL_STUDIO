/**
 * Injected by Vite (define) with the server's IPL_DEV_TOKEN, so every API call
 * can authenticate without the user typing anything. Empty when no token is set.
 */
declare const __IPL_DEV_TOKEN__: string;
